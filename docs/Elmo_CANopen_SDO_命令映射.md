# Elmo CANopen SDO（CoE）两字母命令映射与常用命令表

本文把你在官网找到的 **“Elmo Parameters via SDO Interface (CoE / CANopen)”** 说明，和我们从 [docs/MAN-SIMCR.pdf](docs/MAN-SIMCR.pdf)（SimplIQ Command Reference, ver 4.5）里确认到的命令语义合并，形成一份可直接对照实现/抓包验证的命令清单。

> 适用范围：Elmo 支持“Legacy 2-letter SimplIQ 命令”通过 CANopen SDO 访问的场景。

## 1. 映射规则（官网说明）

### 1.1 两字母命令 → 对象索引（Index）

对任意两字母命令 `<L1><L2>`（大写 A..Z），其对象索引定义为：

$$
\text{Index} = 0x3000 + 26\cdot(\text{ord}(L1)-65) + (\text{ord}(L2)-65)
$$

- `ord('A')=65`，`ord('B')=66` ...

例：
- `MO`：`0x3000 + 26*(M- A) + (O-A) = 0x3146`
- `SP`：`0x31E3`
- `AC`：`0x3002`

### 1.2 子索引（SubIndex）规则

官网说明的关键点：**每个两字母命令都被当作“数组命令”处理**。

- SubIndex `0`：只读，返回该命令允许的最大子索引数（数组长度/alias object）
- SubIndex `x>=1`：
  - 对数组型命令（例如 `BP[x]`、`OL[x]`、`OB[x]`），`x` 就是数组下标
  - 对“标量”命令（例如 `MO`、`SP`、`AC` 等），也以 `CMD[1]` 的形式访问

> 也就是说，**标量命令按官网规则应使用 subIndex=1**；subIndex=0 通常不是“值”。

### 1.3 本工程现状（重要提醒）

你已在现场验证：本工程目前这些 CAN 指令都能正常读写与控制，因此这里建议把结论固化为：

- **标量类命令统一使用 `subIndex=1`**（例如 `MO/SP/AC/DC/PX/...` 都用 `:1`）
- **数组类命令使用其下标作为 subIndex**（例如 `BP[1]` 用 `:1`，`BP[2]` 用 `:2`）

> 说明：subIndex=0 在官网描述里主要用于“数组长度/alias object”等信息查询，通常不是实际值。

## 2. SDO 读写格式（结合本工程实现）

工程采用标准 SDO（8 字节）：

- 读（Upload Request）：`0x40`
- 写 4 字节（Download 4-byte）：`0x22`（小端）

注意：很多命令的数值类型不一定都是 32-bit（例如 bit-field、16-bit 等）。本工程当前实现“固定 4 字节写”，对非 32-bit 命令可能需要扩展（`0x2F/0x2B/0x23`）。

## 3. 常用命令映射表（本工程已用 + 手册补齐）

下面给出“命令 → Index/SubIndex”的对照。你已确认 subIndex=1 可以稳定工作，因此本表按 **标量=1、数组=下标** 的规则输出。

> 记号：`Index:Sub` 例如 `0x3146:1`

### 3.1 运动/使能（工程已用）

| 命令 | 含义 | Index | SubIndex | 读写 |
|---|---|---:|---:|---|
| `MO` | Motor On（使能/去使能） | `0x3146` | `1` | R/W |
| `SP` | 速度给定 | `0x31E3` | `1` | R/W |
| `AC` | 加速度给定 | `0x3002` | `1` | R/W |
| `DC` | 减速度给定 | `0x3050` | `1` | R/W |
| `PR` | 相对位置给定 | `0x3197` | `1` | R/W |
| `PA` | 绝对位置给定 | `0x3186` | `1` | R/W |
| `BG` | Begin Motion（开始执行最新运动） | `0x3020` | `1`（写任意值触发，常用 1） | W |

### 3.2 反馈/状态（工程已用）

| 命令 | 含义 | Index | SubIndex | 读写 |
|---|---|---:|---:|---|
| `PX` | 位置反馈 | `0x319D` | `1` | R |
| `VX` | 速度反馈 | `0x3239` | `1` | R |
| `IQ` | 电流反馈 | `0x30E0` | `1` | R |
| `SO` | 状态字 | `0x31E2` | `1` | R |
| `EC` | 错误码（解释器错误） | `0x306A` | `1` | R |

### 3.3 抱闸（手册 MAN-SIMCR 确认）

> 结论：抱闸不是“单一命令”，而是 **输出映射（OL） + 抱闸时序（BP）**。

| 命令 | 含义 | Index | SubIndex | 读写 | 备注 |
|---|---|---:|---:|---|---|
| `OL[N]` | 输出逻辑/功能映射（含 Brake 功能=2） | `0x3177` | `N` | R/W | 映射为 Brake 后，OB/OP 对该输出不再生效（手册说明） |
| `BP[N]` | 抱闸时序参数（`BP[1]`,`BP[2]`） | `0x3029` | `1..2` | R/W | 手册要求 `MO=0` 才允许修改；范围 0..500ms |
| `OB[N]` | 输出位数组/状态（含 `OB[8]` 抱闸状态） | `0x316D` | `N` | R/W | 手册：`OB[8]=1` 抱闸已抱紧，`0` 释放（前提：至少一路输出映射为 Brake） |


#### 3.3.1 抱闸应该发哪些指令？怎么控制？（你问的第 3 点）

结论：抱闸不是“单独一个开关命令”，而是 **绑定在 MO 的自动行为**：

- 你需要配置：`OL[N]`（把某路数字输出映射为 Brake）+ `BP[1]`/`BP[2]`（抱闸时序）
- 你需要控制：用 `MO=1` / `MO=0` 来触发抱闸“释放/抱紧”
- 你可以确认：读 `OB[8]` 判断抱闸是否已经抱紧/释放

推荐流程（全程按 subIndex 规则：标量=1，数组=下标）：

1) 配置阶段（必须 `MO=0`）

- 选择一个数字输出通道 N（例如 N=2）
- 写 `OL[N]`：`0x3177:N = 0x00000005`（示例值）
  - 手册表 3-34 给出：bit0=逻辑（0=low active，1=high active），bit1..4=功能（2=Brake）
  - 因此示例值 `1 + (2<<1) = 5`（建议写完再读回确认）
- 写 `BP[1]`：`0x3029:1 = <delay_ms>`（0..500）
- 写 `BP[2]`：`0x3029:2 = <delay_ms>`（0..500）

2) 运行阶段（控制抱闸）

- 释放抱闸：写 `MO=1` → `0x3146:1 = 1`
- 抱紧抱闸：写 `MO=0` → `0x3146:1 = 0`
- 查询抱闸状态：读 `OB[8]` → `0x316D:8`

> 手册还提到：如果是“实时 emergency 禁能”，抱闸会在禁能瞬间动作，电机可能先 freewheel 到抱闸完全介入；这点要结合机械安全评估。

### 3.4 停机/急停（手册 MAN-SIMCR + 官网 quick stop 说明）

| 命令 | 含义 | Index | SubIndex | 读写 | 备注 |
|---|---|---:|---:|---|---|
| `SD` | Stop Deceleration（紧急停机减速度基准） | `0x31D7` | `1` | R/W | 手册：用于 `ST`/硬件 stop/限位/abort 等触发的减速 |
| `ST` | Stop Motion（软件受控减速停） | `0x31E7` | `1`（写任意值触发，常用 1） | W | 手册：按 `SD` 减速到 0；不影响外部参考 |
| `KL` | Kill Motion and Program（停程序+`MO=0`） | `0x310F` | `1`（写任意值触发） | W | 手册：会执行 `MO=0`，后续 `XC` 续跑可能失败 |
| `HP` | Halt Program Execution（仅停程序） | `0x30C5` | `1`（写任意值触发） | W | 手册：冻结状态，不 reset；可 `XC` 继续 |
| `MO=0` | 去使能（更接近“立刻停”） | `0x3146` | `1` | W | 若启用抱闸，停机过程受 `BP[]` 影响（手册说明） |
| DS402 Quick Stop | DS402 状态机 Quick Stop | `0x6040` | n/a | R/W | 手册在 `BG`/错误码里明确提到：quick stop 状态会阻塞 `BG`；由 CAN 主站通过 `0x6040` 控制 |

#### 3.4.1 使用 ST 停止，SD 怎么设置和读取？（你问的第 4 点）

1) 读/写 SD（Stop Deceleration）

- 写（建议 `MO=0` 时写入）：`SD` → `0x31D7:1 = <value>`
- 读：`SD` → 读 `0x31D7:1`
- 单位：counts/sec$^2$（与位置/速度 counts 体系一致）

2) 触发 ST（Stop Motion）

- 写：`ST` → `0x31E7:1 = 1`（写任意值即可，推荐写 1）
- 行为：软件运动按 `SD` 设定减速度减速到 0（手册说明：不影响外部参考输入）

3) SD 的取值建议

- 原则：从保守值开始，逐步上调，观察停止距离、机械冲击、以及 `EC/MF` 是否异常
- 过大风险：位置误差过大触发保护/不稳定；过小风险：停不下来/停得太慢

### 3.5 复位/故障相关（手册 MAN-SIMCR 确认）

| 命令 | 含义 | Index | SubIndex | 读写 | 备注 |
|---|---|---:|---:|---|---|
| `RS` | Soft Reset（恢复出厂默认 + 清 volatile） | `0x31CC` | `1`（写任意值触发） | W | 手册限制：`MO=0` 且 Program not running；不会改 flash，需要 `SV` 才持久化 |
| `MF` | Motor fault（最后一次禁能原因） | `0x313D` | `1` | R | 该命令在手册中用于解释 `EC=66` 等情况；是否在你驱动上可读需实测 |

> 注意：上表 `MF` 的 Index 按公式计算得到（`M`=`12`，`F`=`5`，`0x3000+26*12+5=0x313D`）。

## 4. 给你现有工程的“最小增量建议”

如果你只想尽快把新能力接入现有 CAN SDO：

1) **subIndex 规则**：你已验证 `sub=1` 工作正常，建议保持“标量=1、数组=下标”。
2) 增加以下命令的 SDO 封装：
   - 抱闸：`OL[N]`、`BP[1..2]`、`OB[8]`
   - 停机：`SD`、`ST`、`KL`、`HP`
   - 复位：`RS`

## 6. 更多常用读写控制指令（手册 MAN-SIMCR 可查）

下面这些命令在 MAN-SIMCR 中都有条目，且遵循同样的两字母映射规则；适合你在 CAN SDO 层继续扩展。

### 6.1 模式/运动相关

| 命令 | 含义（简述） | Index | SubIndex | 读写 |
|---|---|---:|---:|---|
| `UM` | Unit Mode（控制/运行模式） | `0x3214` | `1` | R/W |
| `JV` | Jog / 速度类运动命令 | `0x30FF` | `1` | R/W |
| `SF` | Smooth factor（平滑因子） | `0x31D9` | `1` | R/W |
| `SR` | Status report（位域/状态） | `0x31E5` | `1` | R |
| `CD` | 诊断/转储信息 | `0x3037` | `1` | R |

### 6.2 I/O 与事件（对限位/急停输入绑定有用）

| 命令 | 含义（简述） | Index | SubIndex | 读写 | 说明 |
|---|---|---:|---:|---|---|
| `IP` | 读取所有数字输入 | `0x30DF` | `1` | R | 用于确认限位/急停输入状态 |
| `IL[N]` | 输入逻辑/功能映射 | `0x30DB` | `N` | R/W | 手册描述可用于 hard stop/soft stop/abort 等功能绑定 |
| `ER[N]` | 误差阈值/保护相关数组 | `0x3079` | `N` | R/W | 修改前建议先读回备份 |
| `GS[N]` | 系统级参数数组 | `0x30AE` | `N` | R/W | 系统级参数，谨慎修改 |
| `WS[N]` | Wizard/系统参数数组 | `0x324E` | `N` | R/W | 系统/采样等参数，谨慎修改 |
| `OP` | Set all digital outputs（设置所有通用输出位域） | `0x317B` | `1` | R/W | 对被映射为 AOK/Brake 的输出不生效（手册说明） |
| `OB[N]` | 输出位数组/状态（含 `OB[8]` 抱闸状态） | `0x316D` | `N` | R/W | 对被映射为 Brake 的输出，写入不生效，但读状态仍有用 |

### 6.3 参数保存/装载

| 命令 | 含义（简述） | Index | SubIndex | 读写 | 说明 |
|---|---|---:|---:|---|---|
| `LD` | 从 Flash 载入参数 | `0x3121` | `1` | W | 写任意值触发（建议写 1） |
| `SV` | 保存参数到 Flash | `0x31E9` | `1` | W | 写任意值触发（建议写 1）；会影响上电默认 |

### 6.4 回零/捕获（如需）

| 命令 | 含义（简述） | Index | SubIndex | 读写 |
|---|---|---:|---:|---|
| `HM[N]` | Homing / capture 参数数组 | `0x30C2` | `N` | R/W |
| `HY[N]` | Homing 辅助参数数组 | `0x30CE` | `N` | R/W |

如果你确认现场走 DS402（有主站状态机）：
- 需要把 `0x6040/0x6041` 的状态机处理纳入控制流程，否则会遇到“Quick stop active，BG 被阻塞”等现象。

## 5. 附：快速算 Index 的小脚本

```python
def elmo_index(cmd: str) -> int:
    cmd = cmd.strip().upper()
    assert len(cmd) == 2 and cmd.isalpha()
    l1, l2 = cmd
    return 0x3000 + 26*(ord(l1) - 65) + (ord(l2) - 65)

print(hex(elmo_index('MO')))  # 0x3146
print(hex(elmo_index('BP')))  # 0x3029
```
