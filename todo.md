根据现在有的状态机设计，完善架构：
1. 根据功能需求完整状态机，可以添加事件,将所需要的参数注入（或者全局变量，将状态机加进入也行）等
2. 搭建一个模式功能框架，只需要框架，功能实现先备注留出来出来，暂时不实现功能部分
3. 尽量精简，简单的实现不要去套一个函数。允许一个结构体把需要用的全局变量都写进去，而不是去获取查询
4. 内部流程尽量用状态机而不是switch case 
功能如下（根据功能搭建主要架构）：
1. 主要的外设：电机。控制方式可以调用我的函数 ElmoOps->xxx 。根据不同的模式，命令控制方式不同，但是接收解析处理都在CAN中断，或者主循环的轮询。时基间间隔为0.1ms，来自定时器（后续我来实现）
2. 主要的模式：1、校准标定模式 2、位置控制模式 3、压力控制模式
2.1 ElmoOps的控制：校准标定模式在主循环中查询位置、电流、速度；位置控制模式在主循环中查询位置、电流；轮询查询时间间隔50ms；压力控制模式在定时器中断中（压力算法调用的位置）查询位置。
2.2 模式切换：上电后必须完成一次校准标定模式（由外部按键或指令触发），然后才能切换到其他模式。在校准标定模式下，标定未完成或者标定失败，不能切换到其他模式。位置模式和压力模式可以随时切换。
3. 校准模式：
3.1 设置电机到一个较低速度
3.2 设置一个较小的位置，判断当前电流、速度，满足阈值时，认为到达末端，记录位置
3.3 设置一个较大的位置，判断当前电流、速度，满足阈值时，认为到达另外一个末端，记录位置
3.4 检测位置是否满足阈值，满足则认为校准成功，否则失败。成功后速度恢复到正常值
3.5 校准超时为2分钟
4. 位置控制模式：
4.1 任意位置，可以设置到任何一个位置点
4.2 有全开和全关命令
5. 压力控制模式：
5.1 可以设置任意压力
5.2 压力控制模式下，运行在0.1ms定时器中，暂定9ms一个周期，在定时器中断完成算计算，ElmoOps的控制。
6. 需要有状态监测的功能，在主循环中

